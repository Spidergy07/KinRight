# Images

Put production image assets here when the app needs committed UI images.

Use `local-images/` for private screenshots, food photos, and temporary API test uploads. That folder is ignored by git.
